/*
#include<stdio.h>
#include<stdlib.h>

int main()
{
	int a[10] = { 0 };
	printf("sizeof(a) = %d\n", sizeof(a));		//打印a的长度
	printf("a : %d    &a : %d    &a[0] : %d", a, &a, &a[0]);		//a的首地址、整个a的地址、a的首元素的地址
	printf("\n");
	printf("a+1 : %d    &a+1 : %d    &a[0]+1 : %d", a + 1, &a + 1, &a[0] + 1);
	//数组首地址+1则是下一个元素的地址，整个数组的地址+1则是整个数组后的第一个地址。
	printf("\n");
	return 0;
}
*/